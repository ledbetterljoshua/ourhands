{
  "private": true,
  "name": "ourhands",
  "scripts": {
    "build:server": "lerna run build --scope=@ourhands/server",
    "build:web": "lerna run build --scope={@ourhands/controller,@ourhands/web}",
    "build:web-prod": "lerna run build --scope={@ourhands/common,@ourhands/web}"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/rimraf",
      "**/rimraf/**"
    ]
  },
  "devDependencies": {
    "lerna": "^3.17.0"
  }
}
